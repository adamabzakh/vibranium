name: app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.9.0-164.0.dev

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  http:
  provider:
  uuid:
  video_player:
  intl:
  firebase_core:
  screen_protector:
  firebase_messaging:
  permission_handler: ^12.0.1
  lazy_load_scrollview: ^1.3.0
  ticketcher: ^1.1.0
  firebase_in_app_messaging:
  package_info_plus: ^8.0.2
  url_launcher: ^6.3.0
  firebase_app_installations:
  barcode_widget:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/
    - assets/json/pc_layout_stations_v1.json
    - assets/lounge/drinks.png
    - assets/lounge/coffee.png
    - assets/lounge/food.png
